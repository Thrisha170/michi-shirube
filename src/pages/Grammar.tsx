 import { SectionPage } from './SectionPage';
 
 const Grammar = () => (
   <SectionPage
     section="grammar"
     title="Grammar"
     titleJp="文法"
     color="hsl(var(--grammar))"
     unit="points"
   />
 );
 
 export default Grammar;